{
  "swagger": "2.0",
  "info": {
    "description": "",
    "version": "1.0.0",
    "title": "PROJECT-FIBER",
    "contact": {
      "email": "oluwabukolatina@gmail.com"
    }
  },
  "basePath": "/api/v1/dummy",
  "tags": [
    {
      "name": "project-fiber",
      "description": "Everything about theproject"
    }
  ],
  "schemes": ["https"],
  "paths": {
    "/clients": {
      "post": {
        "tags": ["client"],
        "summary": "allows client to add a new entry in the database",
        "description": "",
        "operationId": "createClient",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "description": "client to be created",
            "required": true,
            "schema": {
              "$ref": "#/definitions/Client"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "client created"
          }
        }
      }
    },
    "/client/{clientId}": {
      "put": {
        "tags": ["client"],
        "summary": "edit client",
        "description": "",
        "operationId": "edit-client",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "path",
            "name": "clientId",
            "description": "id of client to be edited",
            "required": true,
            "type": "string",
            "schema": {
              "$ref": "#/definitions/Client"
            }
          },
          {
            "in": "body",
            "name": "body",
            "description": "client to be edited",
            "required": true,
            "schema": {
              "$ref": "#/definitions/Client"
            }
          }
        ],
        "responses": {
          "400": {
            "description": "Could not edit client"
          },
          "200": {
            "description": "client updated"
          },
          "500": {
            "description": "server error"
          }
        }
      }
    }
  },
  "definitions": {
    "Client": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "example": "Add exmple"
        }
      }
    },
    "ApiResponse": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "type": {
          "type": "string"
        },
        "status": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      }
    }
  },
  "externalDocs": {
    "description": "Find out more about project fiber",
    "url": ""
  }
}
